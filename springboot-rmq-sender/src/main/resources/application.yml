server:
  port: 9095
spring:
  application:
    name: springboot-rmq-sender
  rabbitmq:
    host: rabbitmq-cluster.rabbitmq-system.svc.cluster.local
    username: rabbitmq
    password: rabbitmq

management:
  endpoints:
    web:
      exposure:
        include: "*"      
